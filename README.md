# Questions and Answers API

API-сервис для управления вопросами и ответами, построенный на FastAPI с использованием PostgreSQL.

## 📋 Описание проекта

Сервис предоставляет REST API для создания и управления вопросами и ответами. Реализованы все необходимые CRUD операции с валидацией данных, каскадным удалением и логированием.

## 🏗️ Архитектура проекта

```
que-ans-api/
├── app/
│   ├── __init__.py
│   ├── main.py                 # Точка входа приложения
│   │
│   ├── api/                    # API слой с версионированием
│   │   ├── __init__.py
│   │   └── v1/                 # API версия 1
│   │       ├── __init__.py     # Главный роутер v1
│   │       ├── questions.py   # Роутеры для вопросов
│   │       └── answers.py      # Роутеры для ответов
│   │
│   ├── domains/                # Доменные модули (Domain-Driven Design)
│   │   ├── __init__.py
│   │   ├── questions/          # Доменный модуль вопросов
│   │   │   ├── __init__.py
│   │   │   ├── model.py        # SQLAlchemy модель
│   │   │   ├── repository.py  # Репозиторий для работы с БД
│   │   │   ├── service.py      # Бизнес-логика
│   │   │   └── schemas.py     # Pydantic схемы
│   │   └── answers/            # Доменный модуль ответов
│   │       ├── __init__.py
│   │       ├── model.py
│   │       ├── repository.py
│   │       ├── service.py
│   │       └── schemas.py
│   │
│   ├── core/                   # Ядро приложения
│   │   ├── __init__.py
│   │   ├── base_model.py      # Базовая модель
│   │   ├── base_repository.py # Базовый репозиторий
│   │   ├── database.py        # Подключение к БД
│   │   ├── dependencies.py    # Зависимости (DI)
│   │   └── schemas.py         # Общие схемы (StandardResponse)
│   │
│   └── utils/                  # Утилиты
│       ├── __init__.py
│       ├── config.py          # Настройки приложения
│       └── logger.py         # Настройка логирования
│
├── alembic/                    # Миграции базы данных
│   ├── versions/
│   │   └── f69254853498_initial.py
│   ├── env.py
│   └── script.py.mako
│
├── tests/                      # Тесты
│   ├── __init__.py
│   ├── conftest.py
│   ├── test_questions.py
│   └── test_answers.py
│
├── Dockerfile
├── docker-compose.yml
├── requirements.txt
├── alembic.ini
├── pytest.ini
├── run_migrations.sh           # Скрипт для запуска миграций
└── README.md
```

## 🚀 Быстрый старт

### Требования

- Docker и Docker Compose
- Git (для клонирования репозитория)

### Запуск через Docker Compose

1. Клонируйте репозиторий:
```bash
git clone <repository-url>
cd que-ans-api
```

2. Запустите приложение:
```bash
docker-compose up --build
```

Приложение будет доступно по адресу: `http://localhost:8000`

API документация (Swagger UI): `http://localhost:8000/docs`

Альтернативная документация (ReDoc): `http://localhost:8000/redoc`

### Что происходит при запуске:

1. Создается и запускается контейнер PostgreSQL
2. Запускается скрипт `run_migrations.sh`, который выполняет миграции Alembic
3. Запускается FastAPI приложение

## 📚 API Endpoints

> **Примечание:** Все API endpoints версионированы и доступны по префиксу `/api/v1/`

### Вопросы (Questions)

#### GET /api/v1/questions/
Получить список всех вопросов

**Ответ:**
```json
{
  "message": "Questions retrieved successfully",
  "data": [
    {
      "id": 1,
      "text": "Какой язык программирования лучше?",
      "created_at": "2024-01-01T12:00:00",
      "updated_at": "2024-01-01T12:00:00"
    }
  ]
}
```

#### POST /api/v1/questions/
Создать новый вопрос

**Тело запроса:**
```json
{
  "text": "Какой язык программирования лучше?"
}
```

**Ответ:**
```json
{
  "message": "Question created successfully",
  "data": {
    "id": 1,
    "text": "Какой язык программирования лучше?",
    "created_at": "2024-01-01T12:00:00",
    "updated_at": "2024-01-01T12:00:00"
  }
}
```

#### GET /api/v1/questions/{id}
Получить вопрос и все ответы на него

**Ответ:**
```json
{
  "message": "Question retrieved successfully",
  "data": {
    "id": 1,
    "text": "Какой язык программирования лучше?",
    "created_at": "2024-01-01T12:00:00",
    "updated_at": "2024-01-01T12:00:00",
    "answers": [
      {
        "id": 1,
        "question_id": 1,
        "user_id": 123,
        "text": "Python - отличный выбор!",
        "created_at": "2024-01-01T12:05:00",
        "updated_at": "2024-01-01T12:05:00"
      }
    ]
  }
}
```

#### DELETE /api/v1/questions/{id}
Удалить вопрос (вместе с ответами каскадно)

**Ответ:**
```json
{
  "message": "Question deleted successfully",
  "data": {
    "id": 1
  }
}
```

### Ответы (Answers)

#### POST /api/v1/questions/{question_id}/answers/
Добавить ответ к вопросу

**Тело запроса:**
```json
{
  "text": "Python - отличный выбор!",
  "user_id": 123
}
```

**Ответ:**
```json
{
  "message": "Answer created successfully",
  "data": {
    "id": 1,
    "question_id": 1,
    "user_id": 123,
    "text": "Python - отличный выбор!",
    "created_at": "2024-01-01T12:05:00",
    "updated_at": "2024-01-01T12:05:00"
  }
}
```

#### GET /api/v1/answers/{answer_id}
Получить конкретный ответ

**Ответ:**
```json
{
  "message": "Answer retrieved successfully",
  "data": {
    "id": 1,
    "question_id": 1,
    "user_id": 123,
    "text": "Python - отличный выбор!",
    "created_at": "2024-01-01T12:05:00",
    "updated_at": "2024-01-01T12:05:00"
  }
}
```

#### DELETE /api/v1/answers/{answer_id}
Удалить ответ

**Ответ:**
```json
{
  "message": "Answer deleted successfully",
  "data": {
    "id": 1
  }
}
```

## 🧪 Тестирование

Для запуска тестов:

```bash
# Внутри контейнера
docker-compose exec api pytest

# Или локально (требуется установка зависимостей)
pytest
```

Тесты покрывают:
- Создание, получение и удаление вопросов
- Создание, получение и удаление ответов
- Валидацию входных данных
- Каскадное удаление ответов при удалении вопроса
- Обработку ошибок (404 для несуществующих ресурсов)

## 🔧 Технические детали

### Используемые технологии

- **FastAPI** - современный веб-фреймворк для создания API
- **SQLAlchemy** - ORM для работы с базой данных
- **PostgreSQL** - реляционная база данных
- **Alembic** - система миграций
- **Pydantic** - валидация данных и сериализация
- **Pytest** - фреймворк для тестирования
- **Docker** - контейнеризация приложения

### Особенности реализации

1. **Версионирование API**: Структура поддерживает версионирование API через `/api/v1/`, что позволяет легко добавлять новые версии без нарушения обратной совместимости
2. **Domain-Driven Design**: Проект организован по доменным модулям в папке `app/domains/`. Каждый домен (questions, answers) изолирован и содержит:
   - **Модель** (`model.py`) - SQLAlchemy модель для работы с БД
   - **Репозиторий** (`repository.py`) - слой доступа к данным
   - **Сервис** (`service.py`) - бизнес-логика домена
   - **Схемы** (`schemas.py`) - Pydantic схемы для валидации и сериализации
3. **Слоистая архитектура**: Четкое разделение на слои:
   - **API слой** (`app/api/v1/`) - HTTP endpoints и роутинг
   - **Доменный слой** (`app/domains/`) - бизнес-логика и модели доменов
   - **Сервисный слой** (`domains/*/service.py`) - бизнес-логика доменов
   - **Слой репозиториев** (`domains/*/repository.py`) - доступ к данным
   - **Слой моделей** (`domains/*/model.py`) - структура данных БД
4. **Инъекция зависимостей**: Использование FastAPI Depends для внедрения репозиториев и сессий БД
5. **Валидация данных**: Pydantic схемы с проверкой на пустые строки и корректность данных
6. **Каскадное удаление**: При удалении вопроса автоматически удаляются все связанные ответы
7. **Стандартизированные ответы**: Все ответы API обернуты в `StandardResponse` для единообразия
8. **Логирование**: Настроено логирование всех операций
9. **Типизация**: Полная типизация кода с использованием type hints
10. **Базовые классы**: Использование `BaseRepository` и `BaseModel` для переиспользования кода

### База данных

Модели:
- **Question**: id, text, created_at, updated_at
- **Answer**: id, question_id (FK), user_id, text, created_at, updated_at

Связи:
- Один вопрос может иметь множество ответов (One-to-Many)
- При удалении вопроса каскадно удаляются все ответы (CASCADE DELETE)
- Все модели наследуются от `BaseModel` с общими полями (id, created_at, updated_at)

### Миграции

Миграции выполняются автоматически при запуске через скрипт `run_migrations.sh`.

Для создания новой миграции:
```bash
docker-compose exec api alembic revision --autogenerate -m "Описание миграции"
docker-compose exec api alembic upgrade head
```

## 🔐 Переменные окружения

Можно создать файл `.env` для настройки:

```env
DATABASE_URL=postgresql://postgres:postgres@db:5432/que_ans_db
DEBUG=False
```

## 📝 Примеры использования

### Создание вопроса и ответа

```bash
# Создать вопрос
curl -X POST "http://localhost:8000/api/v1/questions/" \
  -H "Content-Type: application/json" \
  -d '{"text": "Какой язык программирования лучше?"}'

# Создать ответ
curl -X POST "http://localhost:8000/api/v1/questions/1/answers/" \
  -H "Content-Type: application/json" \
  -d '{"text": "Python!", "user_id": 123}'

# Получить список всех вопросов
curl "http://localhost:8000/api/v1/questions/"

# Получить вопрос с ответами
curl "http://localhost:8000/api/v1/questions/1"

# Получить конкретный ответ
curl "http://localhost:8000/api/v1/answers/1"

# Удалить ответ
curl -X DELETE "http://localhost:8000/api/v1/answers/1"

# Удалить вопрос (каскадно удалятся все ответы)
curl -X DELETE "http://localhost:8000/api/v1/questions/1"
```

## 🐛 Решение проблем

### Проблема: База данных не готова

Если миграции не выполняются, убедитесь, что PostgreSQL контейнер запущен и здоров:
```bash
docker-compose ps
```

### Проблема: Порт уже занят

Измените порт в `docker-compose.yml`:
```yaml
ports:
  - "8001:8000"  # Вместо 8000:8000
```

## 📄 Лицензия

Этот проект создан в рамках тестового задания.

